{
  "name": "plant-care-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:mongo && concurrently -n api,web -c blue,green \"npm run dev -w @plant-care/api\" \"wait-on http://localhost:3001/api/health -t 60000 && npm run dev -w @plant-care/web\"",
    "dev:mongo": "docker compose up -d",
    "dev:apps": "concurrently -n api,web -c blue,green \"npm run dev -w @plant-care/api\" \"npm run dev -w @plant-care/web\"",
    "lint": "npm run lint --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@llm/linting": "workspace:*",
    "concurrently": "^9.1.2",
    "eslint": "^9.39.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "wait-on": "^8.0.3"
  },
  "prettier": "@llm/linting/prettier",
  "engines": {
    "node": ">=22"
  }
}
